//To start off, the form built in html needs to be linked to .js

 const form= document.querySelector("#careerForm")

 formInteract.addEventListener("submit", function(event) {
  event.preventDefault();
});